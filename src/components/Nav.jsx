import SearchBar from './SearchBar';

function Nav({ onSearch }) {
	return (
		<nav>
			<div />
			<div />
			<div />
			<h1>Weather App</h1>
			<SearchBar onSearch={onSearch} />
		</nav>
	);
}

export default Nav;
